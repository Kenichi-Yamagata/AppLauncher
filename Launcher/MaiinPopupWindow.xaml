<Popup x:Class="Launcher.MaiinPopupWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Launcher"
             mc:Ignorable="d" 
             >
    <Border Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="3" Padding="5">
        <TreeView x:Name="_treeView" 
                    MaxWidth="600" 
                    MaxHeight="600"
                    MinWidth="200"
                    MinHeight="100"
                    Background="White"
                    ItemsSource="{Binding Items}"
                  >
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal">
                        <Image Source="{Binding Icon}" Width="24" Height="24" Margin="0,0,4,0" />
                        <TextBlock Text="{Binding Name}" FontSize="18" />
                    </StackPanel>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="FontWeight" Value="Normal" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="FontWeight" Value="Bold" />
                        </Trigger>
                    </Style.Triggers>
                    <!-- InputBindingを使用してクリックをコマンドに変換 -->
                    <!--
                    <Setter Property="InputBindings">
                        <Setter.Value>
                            <InputBindingCollection>
                                <MouseBinding 
                                    MouseAction="LeftClick" 
                                    Command="{Binding DataContext.ItemClickCommand, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type TreeView}}}" 
                                    CommandParameter="{Binding}" />
                            </InputBindingCollection>
                        </Setter.Value>
                    </Setter>
                    -->
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Border>
</Popup>
